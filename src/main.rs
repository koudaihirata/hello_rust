use std::f64;

fn main() {
    let t1: (i32, bool, f64) = (1, true, 2.0);

    println!("{:?}",t1);

    let i = t1.0;
    println!("{}",i);

    // let (x: f64, y: i32, _) = t2;

    // let u = ();
}